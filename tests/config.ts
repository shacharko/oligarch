/**
 * Fill in these handles before running the tests.
 * A handle is the URL slug shown after /products/ or /collections/.
 *
 * Example: for https://oligarch.co.il/products/byredo-sample-set  →  handle = 'byredo-sample-set'
 */
export const HANDLES = {
  /** Build-your-own kit product that uses the kit-selector template (multi-select picker) */
  kitSelector: 'byredo-sample-set',
};
